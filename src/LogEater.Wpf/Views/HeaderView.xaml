<UserControl x:Class="LogEater.Wpf.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:LogEater.Wpf.ViewModels"
             d:DataContext="{d:DesignInstance viewModels:HeaderViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="600">
    <Grid Background="{StaticResource HeaderFooterBackgroundColor}">
        <TextBlock x:Name="LogFile"
                   Style="{StaticResource Big}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center" />

        <Border BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0 0 0 1" />
    </Grid>
</UserControl>